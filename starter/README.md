# Starter

Pequeño ejemplo de la plataforma de streaming en TypeScript.

Consulta el código fuente en `starter/index.ts` para ver la implementación.

Instrucciones:

1. Instalar dependencias: `pnpm install`
2. Ejecutar: `pnpm start`

